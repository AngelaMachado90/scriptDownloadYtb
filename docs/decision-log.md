# Decision log

- Dados de mídia permanecem em `downloads/` e nunca fazem parte do código versionado.
- Entry points finos preservam CLI e Streamlit enquanto a lógica fica reutilizável em `music_library/`.
